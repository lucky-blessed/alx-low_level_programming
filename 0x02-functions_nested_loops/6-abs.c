#include "main.h"
/**
 * _abs - Prints absolute value of a number
 * @n: The number to be printed to print its absolute value
 * Return: Absolute value of n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
